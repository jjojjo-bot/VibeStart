{
  "name": "@vibestart/track-catalog",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vibestart/ports": "workspace:*",
    "@vibestart/shared-types": "workspace:*"
  }
}
